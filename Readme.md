just take a look at id3.mjs, specifically the top comment block. you can likely just drop it in your node or browser project as is and have it work. note that I dev'd in node so browser is likely less stable, but stability is in no way garaunteed anyway.
test.html in vscode integrated browser passes on my machine.
node ID3.mjs passes on my machine, node version v20.20.2.